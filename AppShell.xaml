<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DayPlanner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.FlyoutBehavior="Flyout"
    xmlns:views="clr-namespace:DayPlanner.Views"
    Title="Заметки">

    <ShellContent 
        Title="Заметки" 
        ContentTemplate="{DataTemplate views:NoteListPage}" 
        Route="NoteListPage"/>
    <ShellContent 
        Title="Создать/Редактировать заметку" 
        ContentTemplate="{DataTemplate views:NoteEditPage}" 
        Route="NoteEditPage"/>

</Shell>
